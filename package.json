{
  "name": "workwork",
  "version": "1.0.0",
  "private": true,
  "description": "Monorepo project with React frontend and NestJS backend",
  "workspaces": [
    "browser",
    "server"
  ],
  "scripts": {
    "dev:browser": "npm run dev --workspace=browser",
    "dev:server": "NODE_ENV=development npm run start:dev --workspace=server",
    "dev": "concurrently \"npm run dev:browser\" \"npm run dev:server\"",
    "build:browser": "npm run build --workspace=browser",
    "build:server": "npm run build --workspace=server",
    "build": "npm run build:browser && npm run build:server",
    "clean": "npm run clean --workspaces",
    "clean:dist": "rm -rf server/dist browser/dist server/*.tsbuildinfo",
    "clean:modules": "rm -rf node_modules server/node_modules browser/node_modules"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

